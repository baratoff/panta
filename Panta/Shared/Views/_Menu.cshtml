
<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Panta Admin</a>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
            <ul class="nav navbar-nav">
                <li class="dropdown @(ViewContext.RouteData.Values["Controller"].ToString() == "Company" ? "active" : "")">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">Company<span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="@Url.Action("Create", "Company")">Create</a></li>
                        <li><a href="@Url.Action("Edit", "Company")">Edit</a></li>
                    </ul>
                </li>
                <li class="dropdown @(ViewContext.RouteData.Values["Controller"].ToString() == "Booking" ? "active" : "")">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">Booking<span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="@Url.Action("Index", "Booking")">Index</a></li>
                        <li><a href="@Url.Action("CreateResource", "Booking")">CreateResource</a></li>
                    </ul>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="#"><span class="glyphicon glyphicon-cog"></span> Settings</a></li>
            </ul>
        </div>
    </div>
</nav>